{% extends 'base.html.twig' %}

{% block title %}Nos Produits{% endblock %}

{% block content %}
    <h1> Nos Produits</h1>
    <div class="row">
    {% for product in products %}
       <div class="col-md-4">
<div class="product-item">
    <img src="uploads/{{product.illustration}}" alt="img-fluid">
    <h5>{{product.name}}</h5>
    <span>{{product.subtitle}}</span>

</div>

    </div>
    {% endfor %}
    </div>
{% endblock content %}
